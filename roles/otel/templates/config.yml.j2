receivers:
  otlp/monad-testnet:
    protocols:
      grpc:
        endpoint: '0.0.0.0:{{ otel.ports.monad_testnet.grpc_port }}'
      http:
        endpoint: '0.0.0.0:{{ otel.ports.monad_testnet.http_port }}'
exporters:
  prometheus/monad:
    endpoint: '0.0.0.0:{{ otel.ports.prometheus_metrics_exporter_port }}'
    send_timestamps: true
    metric_expiration: 10m
    enable_open_metrics: true
    resource_to_telemetry_conversion:
      enabled: true
    const_labels:
      project: monad
processors:
  resource/monad-testnet:
    attributes:
      - action: insert
        key: env
        value: testnet

service:
  pipelines:
    metrics/monad-testnet:
      receivers: [otlp/monad-testnet]
      processors: [resource/monad-testnet]
      exporters: [prometheus/monad]
  telemetry:
    metrics:
      readers:
        - pull:
            exporter:
              prometheus:
                host: '0.0.0.0'
                port: {{ otel.ports.metrics_port }}
